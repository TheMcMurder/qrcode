{
  "name": "@qrcode/wasm",
  "version": "1.0.0",
  "description": "WebAssembly bindings for QR code generation",
  "type": "module",
  "main": "pkg/qrcode_wasm.js",
  "types": "pkg/qrcode_wasm.d.ts",
  "files": [
    "pkg"
  ],
  "scripts": {
    "build": "wasm-pack build --target web",
    "format": "cargo fmt",
    "clean": "rm -rf .turbo && rm -rf node_modules && rm -rf pkg"
  },
  "keywords": ["qrcode", "wasm", "rust"],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@qrcode/core": "workspace:*"
  },
  "devDependencies": {
    "wasm-pack": "^0.12.0"
  }
} 